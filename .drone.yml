kind: pipeline
type: docker
name: Deploiement

trigger:
  event:
    - push

steps:
  - name: add-base-path
    image: ubuntu:latest
    volumes:
      - name: src
        path: /src
    command:
      - sed -i '1i\<base href="https://codefirst.iut.uca.fr/containers/axelde_la_fuente-portfolio/">' index.html

  - name: docker-build-and-push
    image: plugins/docker
    volumes:
      - name: src
        path: /src     
    settings:
       dockerfile: docker/Dockerfile
       context: .
       registry: hub.codefirst.iut.uca.fr
       repo: hub.codefirst.iut.uca.fr/axel.de_la_fuente/portfolio
       username:
         from_secret: SECRET_REGISTRY_USERNAME
       password:
         from_secret: SECRET_REGISTRY_PASSWORD
    depends_on: [ add-base-path ]

  - name: deploy-container
    image: hub.codefirst.iut.uca.fr/thomas.bellembois/codefirst-dockerproxy-clientdrone:latest
    environment:
        IMAGENAME: hub.codefirst.iut.uca.fr/axel.de_la_fuente/portfolio:latest
        CONTAINERNAME: portfolio
        COMMAND: create
        OVERWRITE: true
    depends_on: [ docker-build-and-push ]